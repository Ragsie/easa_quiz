name: Build & Publish Release

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. Install & Cache Qt
      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.6.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_mingw'
          dir: ${{ runner.temp }}
          cache: 'true'
          cache-key-prefix: '${{ runner.os }}-Qt-6.6.2-mingw64'

      # 2. Configure & Build Project med CMake og Ninja
      - name: Configure CMake
        run: |
          cmake -S code -B build -G "Ninja" -DCMAKE_BUILD_TYPE=Release

      - name: Build Project
        run: |
          cmake --build build --config Release

# 3. Saml Qt DLL'er korrekt
      - name: Collect Dependencies (windeployqt)
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item build\Easa-B2-Quiz.exe dist\quiz_app.exe
          
          # Kør windeployqt med eksplicit plugin- og biblioteks-mapping
          windeployqt --release --no-translations --compiler-runtime dist\quiz_app.exe

      # 4. Pak med avanceret fejlfinding (fanger crash exit codes)
      - name: Package Single-File Standalone EXE
        run: |
          # 1. Pak dist-mappen ned
          & "C:\Program Files\7-Zip\7z.exe" a -r -mx9 app.zip .\dist\*

          # 2. Launcher med crash-diagnostik
          $launcherCode = @"
          using System;
          using System.IO;
          using System.IO.Compression;
          using System.Diagnostics;
          using System.Reflection;
          using System.Windows.Forms;

          class Program {
              [STAThread]
              static void Main() {
                  string tempFolder = Path.Combine(Path.GetTempPath(), "EasaB2Quiz_" + Guid.NewGuid().ToString("N"));
                  try {
                      Directory.CreateDirectory(tempFolder);
                      
                      using (Stream stream = Assembly.GetExecutingAssembly().GetManifestResourceStream("app.zip"))
                      using (ZipArchive archive = new ZipArchive(stream)) {
                          archive.ExtractToDirectory(tempFolder);
                      }

                      string exePath = Path.Combine(tempFolder, "quiz_app.exe");
                      ProcessStartInfo psi = new ProcessStartInfo(exePath) {
                          WorkingDirectory = tempFolder,
                          UseShellExecute = false
                      };

                      using (Process proc = Process.Start(psi)) {
                          proc.WaitForExit();
                          
                          // Hvis programmet crasher (exit code != 0), vis en popup med fejlkoden
                          if (proc.ExitCode != 0) {
                              MessageBox.Show("Programmet lukkede unormalt med fejlkode: 0x" + proc.ExitCode.ToString("X"), 
                                              "EASA Quiz Crash", 
                                              MessageBoxButtons.OK, 
                                              MessageBoxIcon.Warning);
                          }
                      }
                  }
                  catch (Exception ex) {
                      MessageBox.Show(ex.ToString(), "Launcher Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
                  }
                  finally {
                      try {
                          if (Directory.Exists(tempFolder)) {
                              Directory.Delete(tempFolder, true);
                          }
                      }
                      catch { }
                  }
              }
          }
          "@
          [System.IO.File]::WriteAllText("Launcher.cs", $launcherCode, [System.Text.Encoding]::UTF8)

          # 3. Kompiler
          $csc = "C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe"
          & $csc /target:winexe /optimize+ /r:System.Windows.Forms.dll /r:System.IO.Compression.dll /r:System.IO.Compression.FileSystem.dll /resource:app.zip /out:Easa-B2-Quiz.exe Launcher.cs
          
      - name: Self-Sign Executable
        run: |
          $cert = New-SelfSignedCertificate -Type CodeSigningCert -Subject "CN=EASA B2 Quiz" -CertStoreLocation "Cert:\CurrentUser\My"
          $certPath = "Cert:\CurrentUser\My\" + $cert.Thumbprint
          Set-AuthenticodeSignature -FilePath ".\Easa-B2-Quiz.exe" -Certificate (Get-Item $certPath) -TimestampServer "http://timestamp.digicert.com"

      # 6. Upload Artifact
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Easa-B2-Quiz-Windows
          path: Easa-B2-Quiz.exe
          if-no-files-found: error

     # 7. Opret GitHub Release med unikt build-tag
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: Easa-B2-Quiz.exe
          tag_name: build-${{ github.run_number }}
          name: "EASA B2 Quiz - Build ${{ github.run_number }}"
          make_latest: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
